rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura/escrita em todas as coleções do app
    // apenas para usuários autenticados (login feito no sistema).
    match /{collection}/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
